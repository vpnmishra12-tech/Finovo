# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increased maxInstances to 10 to handle higher traffic spikes smoothly in production.
  maxInstances: 10
  # Memory and CPU allocated per instance for fast processing of server actions.
  memory: 512Mi
  cpu: 1
